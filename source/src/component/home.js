import React, {Component} from 'react';
import {
    Carousel,
    message,
    Button,
    Skeleton,
    Switch,
    Card,
    Icon,
    Avatar,
    Timeline
} from 'antd';

const {Meta} = Card;
class Home extends Component {
    constructor(props) {
        super(props)
        this.state = {
            loading: true
        }
    }
}
export default Home;
